# Phase 0.0.64 — the described-world boot door

Status: LANDED, commit stamped below, 2026-09-03. Gate: 5 PASS / 0 FAIL; prior gates unmoved.

Api batch rung of `batch-api-1.md`. Serves the checklist box for the described-world boot; the box stays OPEN at this landing, by this plan's own kind statement.

## Lift kind

SHAPED: one call, one plain object, one running world — the description checked at the door by the landed contract pattern, every default stated, every dial a mechanism the engine really reads (gravity the world field, wind the tick switch, trees the grid rule). The checklist box does NOT flip: spec tables and module choices are not yet mechanisms, and words never outrun code. The box flips when its whole sentence is true.

## Acceptance arithmetic for the phase

Every number was produced by running the exact planned code at plan-writing time. A bad description names every problem in one pass and is refused whole; one description boots twin worlds at a rolled seed; every dial provably reaches its mechanism; the defaults are the stated ones.

- `node scripts/gate.mjs describe` — seeds line, 5 PASS lines, `describe-test: 5 PASS / 0 FAIL`, `describe-test PASS`, exit 0.
- frostline's tail unmoved.

## Tasks

- 0.0.64-1 — the files, verbatim from the trial. -> `task-0.0.64-1-describe.md`

Suggested model: Sonnet 5 — every file's full content is in the plan.
