# Batch order 3 — extractions, run continuous

The third continuous run, under the rules batches 1 and 2 proved and hardened. The owner's approval of this document approves the batch; his word interrupts anywhere.

## The rules of the run

Unchanged from `batch-extractions-2.md`: trial first, plan from the templates, rehearsal green, dispatch without review; each gate its own foreground run with a long timeout, frostline's seeds and verdict captured from one run; landing commits add named files only; README boxes flip in the landing when earned; no hardwired seeds; demos read-only; any moved number, failed hash, unlisted difference, or design question the source cannot answer stops the batch and waits.

## The extractions, in order

All depot carve-outs, the sight/wind pattern — the file moves whole into `src/modules/<name>/`, imports rewritten one level deeper as the single named substitution where imports exist, the depot keeps a one-line front door, and the four bracketing gates (api's fixed hashes, combat, frostline at rolled seeds, old-master) must sit unmoved after every rung. Ordered smallest entanglement first:

1. **0.0.53 specs** (310 lines, no imports) — pure VERBATIM, the spec tables as their own module.
2. **0.0.54 ai** (364 lines, 1 import) — the enemy commander's decisions.
3. **0.0.55 save** (396 lines, 3 imports) — the storage door.
4. **0.0.56 accuracy** (593 lines, 3 imports, one circular with state.js) — the scatter arithmetic. The circle already exists in the depot and is safe by the file's own header; the carve keeps it through the shim. If the module boot order breaks the circle, that is a stop, not a workaround.
5. **0.0.57 mapgen** (1106 lines, 4 imports) — the valley generator.
6. **0.0.58 squads** (980 lines, 4 imports) — the squad brains.
7. **0.0.59 buildlines** (234 lines, 5 imports).
8. **0.0.60 units** (630 lines, 5 imports).
9. **0.0.61 drivers** (598 lines, 7 imports).
10. **0.0.62 muster** (355 lines, 8 imports).
11. **0.0.63 bell** (208 lines, 10 imports) — ruled out of batch 2 as tangled; the shim pattern has since carried ten carves, so it gets its trial. Dirtier than its trial shows is a stop.

**Named as staying behind, a ruling not a miss:** tick (16 imports — the heartbeat), state, sim, boot, api — the engine's spine, not organs; and the deadweight actor brains (tramps, pirates) and fracture — SHAPED lifts with design questions that belong in a sitting with the owner, not inside a continuous run.

Eleven rungs, 0.0.53 through 0.0.63. Anything beyond is a new batch order.

## Known risks, stated now

- These are the depot's big organs; a carve that moves any gate number stops the batch at that rung with the tree as evidence.
- The accuracy/state circle is the one structural unknown; its trial answers before its plan exists.
- Cumulative trial mirrors the landing order, as batch 2's did, so each rung's gates run on a tree carrying every earlier carve.

## The record

- [x] 0.0.53 specs
- [x] 0.0.54 ai
- [x] 0.0.55 save
- [x] 0.0.56 accuracy
- [ ] 0.0.57 mapgen
- [ ] 0.0.58 squads
- [ ] 0.0.59 buildlines
- [ ] 0.0.60 units
- [ ] 0.0.61 drivers
- [ ] 0.0.62 muster
- [ ] 0.0.63 bell
