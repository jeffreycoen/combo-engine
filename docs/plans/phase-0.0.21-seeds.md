# Phase 0.0.21 — the seed picks the valley

Status: LANDED, commit `bb67d8e`, 2026-08-30. Gate: 28 PASS / 0 FAIL.

Landed first as task 0.0.19-2 inside the stamped phase 0.0.19 — a versioning nonconformity corrected by this record: every landing is its own phase. The work: a mission is rules over any seeded map; forces place on double-vetted ground; every valley proves its spawn-to-exit road before a man spawns; the page rolls a fresh seed each load, shows it, and pins it in the address for exact replay.

## Tasks

- 0.0.21-1 — the whole landing. → `task-0.0.21-1-seeds.md`
