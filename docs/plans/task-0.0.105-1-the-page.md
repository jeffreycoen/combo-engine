# Task 0.0.105-1 — the page, the orchestrator's own step

The page step of the order batch-ark-1: `docs/gravitys-ark/index.html` and `docs/gravitys-ark/main.js`, written by the orchestrator as the plan-writer's own trial against the landed galaxy and road, in the shape of `docs/frostline/`. No agent; no brief beyond this record. The walk is in the phase document.

## What was done

1. The page: the warped grid from the render2d module with the pit and every alive world as wells, draw radii chosen so the funnels can be seen (the pit 20,000 m plus the hole's edge, a world 8,000 m), the warp depth dial 1,300 at the page's zooms; the worlds in their pits; the ship; the aim; the panes; the buttons; the keys; the drag and the pinch through the page kit's gestures; a fixed step of 1/60 with a clamped catch-up.
2. The trial against the landed modules, headless: the projection of the pit, the first world, and the gate onto a phone-sized screen; the warp depths; a takeoff and a twenty-second burn. The burn flew the ship through its world's centre, where the wells law blows up, and the takeoff left at 60 m/s: two holes in the road's law from the brief, fixed in this step. The surface pull came out a thousand times the world's gravity: the galaxy's mu formula had left out the distance factor the wells module's pull carries, fixed in this step with the gate's check 2 re-taught; the surface pull now equals the world's gravity, measured.
3. The gate: check 10 re-taught for the band over the excess above the descent burn (`vy = -(v + escapeSpeed(w))`, the load tolerance 1e-6); check 12 added: a hull dropped from 50 m lands clean on the surface and never passes through, a hull coming in hot dies, a takeoff leaves at or above the escape speed. 12 PASS / 0 FAIL twice.
4. The README gains the game's section; the order's box flips; the link is live after the push.

## The commit

```
phase 0.0.105 — the page: the galaxy on the warped grid, the ship, the clocks, LAND, TAKE OFF, a burn

GRAVITY'S ARK, the first road. The first deploy: the galaxy on the render2d grid with the worlds in their pits, the ship, the clocks, LAND, TAKE OFF, and a burn, phone and desktop. Three amendments found by the page's trial: the galaxy's mu carries the distance the wells pull carries; a hull that reaches a surface lands by the band; a takeoff leaves at the escape speed. Gate 12 PASS / 0 FAIL at rolled seeds.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01QSq3kZC3Bk9SC5RwnqYUfs
```

## The report's gate lines

- `node scripts/gravitys-ark-test.mjs`: seeds 2633338206 and a second roll; 12 PASS lines, `gravitys-ark-test: 12 PASS / 0 FAIL`, `gravitys-ark-test PASS`, exit 0, twice.
- Bracket at the landing: gravitys-ark, wells, render2d, pagekit, every tail PASS.
- No branch: the orchestrator's own step in the main tree, landed as its own commit; the link live after the push.
- Amendments carried, each in the night log: the galaxy's mu law; the road's surface arrival, landing band, and takeoff speed; gate checks 2 and 10 re-taught, check 12 added.
