# Phase 0.0.60 — the contract pattern

Status: LANDED, commit `02359b5`, 2026-09-03. Gate: 6 PASS / 0 FAIL; prior gates unmoved.

Harness batch rung of `batch-harness-1.md`. Checklist item served: "The contract pattern: tables declared as data, checked at boot, every problem reported at once" — the box flips in this landing.

## Lift kind

SHAPED: the law is the checklist's words — every row against every rule, every problem in one pass, a bad table never reaches the sim. The code is new. The boot door (assertTables) throws once with the whole report; wiring it into the described-world boot belongs to that box's own rung, said plainly here.

## Acceptance arithmetic for the phase

Every number was produced by running the exact planned code at plan-writing time. Two hundred rolled clean tables report nothing; every planted defect on disjoint fields reported exactly, none extra; floor and ceiling each name their number; the engine's live squad table proves clean under its own contract; a missing table is one plain problem, never a crash.

- `node scripts/gate.mjs contract` prints a seeds line, 6 PASS lines, then `contract-test: 6 PASS / 0 FAIL`, then `contract-test PASS`, exit 0 — at rolled seeds.
- Prior gates bracketing: named in the task's steps with their tails.

## Tasks

- 0.0.60-1 — the files, verbatim from the trial. -> `task-0.0.60-1-contract.md`

Suggested model: Sonnet 5 — every file's full content is in the plan.
